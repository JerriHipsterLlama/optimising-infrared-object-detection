# Archived legacy roots

The contents of this directory preserve the pre-reorganization `python/` and `src/python/` roots for reference. They are not supported import paths and are excluded from the active package, applications, tests, and scripts. New work must use `src/infrared_detection/`.
